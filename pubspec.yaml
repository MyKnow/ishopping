name: ishopping
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  haptic_feedback: ^0.4.2
  camera: ^latest_version
  arcore_flutter_plugin: ^latest_version
  popup_dialog: ^latest_version


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
